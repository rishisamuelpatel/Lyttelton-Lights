{% if current_tags %}
  <h2>{{ 'blogs.blog.tag_page_title' | t: tag: current_tags.first }}</h2>
{% endif %}

{% paginate blog.articles by 5 %}

  <div class="blog-posts">
    {% for article in blog.articles %}
      {% render 'article-list-item', article: article %}
    {% else %}
      <span class="empty">{{ 'blogs.blog.empty' | t }}</span>
    {% endfor %}
  </div>

  {% if paginate.previous or paginate.next %}
    {% render 'pagination', paginate: paginate %}
  {% endif %}

{% endpaginate %}


{% schema %}
  {
    "name": "Blog Template",
    "settings": []
  }
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}
